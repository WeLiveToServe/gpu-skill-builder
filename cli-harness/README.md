# Deprecated CLI Harness Copy

This directory is kept only for older `gpu-skill-builder` paths.

The canonical wrapper implementation lives at:

```powershell
C:\Users\keith\dev\cli-harness
```

Set `CLI_HARNESS_DIR` if the canonical checkout moves. New scripts should call:

- `codex-os.cmd`
- `claude-os.cmd`
- `opencode.cmd`
- `qwen.cmd`
